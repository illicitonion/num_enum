[package]
name = "tests"
version = "0.1.0"
authors = ["Daniel Wagner-Hall <dawagner@gmail.com>"]
edition = "2018"
publish = false

[dependencies]
num_enum = { path = "..", features = ["complex-expressions"] }
